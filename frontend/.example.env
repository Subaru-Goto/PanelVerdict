# Server-side only (045/#143): the browser talks to this app's own /api/*
# proxy routes, which hold these. Nothing here is NEXT_PUBLIC_* — anything
# NEXT_PUBLIC_ ships to every visitor in the client bundle by definition.
API_URL=
API_SHARED_SECRET=
