.env
.DS_Store

# Large binaries — keep source papers as URL citations, not committed blobs
*.pdf

# Python
.venv/
__pycache__/

# Node / Next.js
node_modules/
.next/
.env.local
# 014 manipulation-check raw votes: paid model output, committed with the write-up
# under docs/research/ rather than as loose run artifacts
backend/experiments/out/
