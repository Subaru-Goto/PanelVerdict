[project]
name = "panelverdict-backend"
version = "0.1.0"
description = "PanelVerdict backend — FastAPI API + (later) LangGraph orchestrator and panel pipeline."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.139.2",
    "langchain-openai>=1.3.5",
    "psycopg[binary]>=3.3.4",
    "pydantic-settings>=2.14.2",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.15.22",
]
# Offline data pipeline only (stage 1). NOT runtime deps — the app reads the
# committed joint CSVs with stdlib csv. Install with `uv sync --group pipeline`.
pipeline = [
    "pandas>=2.2.0",
]
